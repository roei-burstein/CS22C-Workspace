
public class RomanStringHasher1 implements Hasher<RomanNumeral> {

	@Override
	public int hash(RomanNumeral r) {
		int k, retVal = 0;

		for (k = 0; k < r.getRomNumStr().length(); k++)
			retVal = 37 * retVal + r.getRomNumStr().charAt(k);

		return retVal;
	}
}
